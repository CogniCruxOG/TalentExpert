# Talent Expert — static site (no build step). Netlify serves the repo root.
[build]
  publish = "."
  command = ""

# Long-cache the versioned assets (they use ?v=N cache-busting in the HTML)
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=604800"
